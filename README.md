# Monorepo boilerplate

Secure Web3 wallet infrastructure with fiat on/off-ramps.

Reth-style monorepo:

- `crates/`: Shared libraries
- `bin/api/`: Axum HTTP server
- `bin/cli/`: CLI tools

Run: `just run-api` → http://localhost:3000
